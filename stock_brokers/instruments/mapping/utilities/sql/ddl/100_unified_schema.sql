-- The schema for data that belongs to no single broker: the unified layer every broker's instruments converge on.
-- `bin/unified/map_instruments` maps into the tables below.

CREATE SCHEMA IF NOT EXISTS unified;
