#!/usr/bin/env python3
"""
Poll the Groww user details every minute and write them to the Redis key `groww:user:details`.

    bin/groww/user/details
    redis-cli GET groww:user:details

Calls `GET https://api.groww.in/v1/user/detail` through `GrowwAPI`, which logs in first if the stored
session is dead. Each response - `timestamp`, `status`, `code` and Groww's user details under `data` (the
API client unwraps Groww's `payload`) - is written as one JSON string with SET, replacing the one before.

Runs until Ctrl+C. A dead session (HTTP 401, or a refusal naming authentication or an expired or invalid
token) is logged in again before the next request; Groww's 403, an endpoint the account is not entitled
to, and 429, a rate limit, are not taken for one. Any other failed poll is logged and leaves
`groww:user:details` unchanged. Exits 1 when the first login, or a login after the session died, fails.
Reads only - it places no orders.
"""

import sys
import json
import time
from pathlib import Path

# Run under the project's virtual environment, and put the project on sys.path. See
# utilities/bootstrap.py for why the check is sys.prefix rather than the interpreter path.
sys.path.insert(0, str(Path(__file__).resolve().parents[3]))
from utilities.bootstrap import run_under_venv

PROJECT_ROOT = run_under_venv(__file__)

USER_DETAIL_URL = "https://api.groww.in/v1/user/detail"
REDIS_KEY = "groww:user:details"
DELAY_SECONDS = 60
TIMEOUT_SECONDS = 10

# What Groww says when the session is no good, lowercased.
AUTHENTICATION_MARKERS = ("unauthori", "authentication", "token expired", "invalid token", "expired token", "jwt")

def is_authentication_error(exception):
    """Whether Groww refused the request because the session is dead, rather than for entitlement or rate."""
    status = str(getattr(exception, 'code', '') or '').strip()
    lowered = str(exception).lower()
    if status in ('403', '429') or 'access forbidden' in lowered:
        return False
    return status == '401' or any(marker in lowered for marker in AUTHENTICATION_MARKERS)

def main():
    """Fetch the user details and write them to groww:user:details every minute, until interrupted."""
    from stock_brokers.api.groww import GrowwAPI
    from utilities.configurations import get_cache, get_logger

    logger = get_logger("groww.details")
    cache = get_cache()
    try:
        groww = GrowwAPI()
    except Exception as exception:
        logger.error(f"Could not log in to Groww: {type(exception).__name__}: {exception}")
        return 1

    try:
        while True:
            try:
                profile = groww.get(url=USER_DETAIL_URL, timeout=TIMEOUT_SECONDS)
                if not isinstance((profile or {}).get('data'), dict):
                    raise RuntimeError(f"Groww answered without user details: {str(profile)[:200]}")
                cache.set(REDIS_KEY, json.dumps(profile))
                logger.info(f"Wrote the user details to {REDIS_KEY}")
            except Exception as exception:
                if is_authentication_error(exception):
                    # A dead session: log in again before sending anything more.
                    logger.warning(f"Groww session expired, logging in again: {exception}")
                    groww = GrowwAPI()
                else:
                    logger.error(f"User details poll failed: {type(exception).__name__}: {exception}")
            time.sleep(DELAY_SECONDS)
    except KeyboardInterrupt:
        logger.info("Stopped")
    return 0

if __name__ == "__main__":
    raise SystemExit(main())
